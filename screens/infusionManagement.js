import React, {useState} from 'react'
import {Text, View, StyleSheet, SafeAreaView, FlatList} from 'react-native'

export default function IMScreen({navigation}){

	return(
		<SafeAreaView>
			<Text>Màn hình quản lý dịch truyền offline</Text>
		</SafeAreaView>
	)
}

const styles = StyleSheet.create({
	container:{
		flex: 1
	}
})
